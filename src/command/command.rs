// use knife_macro::knife_component;

// #[knife_component(name = "GLOBAL_COMMAND", crate_builtin = true)]
// pub struct CommandThread {
//     // root_command: RefCell<Command<'static>>,
// }

// impl CommandThread {
//     pub(crate) async fn launch() {
//         // let root_command = ClapCommand::new("app").arg_required_else_help(true);

//         // global_command().root_command.replace(root_command);
//     }
// }
